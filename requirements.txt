# Meridian 工程 Python 依赖（Phase 0）
# 安装：uv pip install -p .venv/Scripts/python.exe -r requirements.txt

# ---- 运行时 ----
akshare>=1.15          # A股数据源（cn_stock.py 独立适配层）
pandas>=2.2            # 统一 schema DataFrame
pydantic>=2.7          # 配置模型
pydantic-settings>=2.4 # config.py
PyYAML>=6.0            # config/*.yaml 读取

# ---- 开发/构建 ----
maturin>=1.7           # 构建 meridian_core
pytest>=8.2            # Python 测试
